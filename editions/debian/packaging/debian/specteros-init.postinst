#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
    systemctl daemon-reload || true
    systemctl enable specteros-init.service || true
    systemctl start specteros-init.service || true
fi

#DEBHELPER#

exit 0
