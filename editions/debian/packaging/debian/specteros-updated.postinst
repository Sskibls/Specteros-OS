#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
    systemctl daemon-reload || true
    systemctl enable specteros-updated.service || true
    systemctl start specteros-updated.service || true
fi

#DEBHELPER#

exit 0
