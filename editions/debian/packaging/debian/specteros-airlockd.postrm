#!/bin/sh
set -e

if [ "$1" = "remove" ]; then
    systemctl stop specteros-airlockd.service || true
    systemctl disable specteros-airlockd.service || true
fi

if [ "$1" = "purge" ]; then
    systemctl daemon-reload || true
fi

#DEBHELPER#

exit 0
