#!/bin/sh
set -e

if [ "$1" = "remove" ]; then
    systemctl stop specteros-auditd.service || true
    systemctl disable specteros-auditd.service || true
fi

if [ "$1" = "purge" ]; then
    systemctl daemon-reload || true
fi

#DEBHELPER#

exit 0
